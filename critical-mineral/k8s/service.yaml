apiVersion: v1
kind: Service
metadata:
  name: mineralinsight-frontend-service
  namespace: mineralinsight
  labels:
    app: mineralinsight-frontend
spec:
  selector:
    app: mineralinsight-frontend
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
